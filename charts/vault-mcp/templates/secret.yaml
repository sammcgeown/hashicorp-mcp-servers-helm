{{- if .Values.vaultSecret.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.vaultSecret.name }}
  labels:
    {{- include "vault-mcp.labels" . | nindent 4 }}
type: Opaque
stringData:
  VAULT_TOKEN: {{ .Values.vaultSecret.token | quote }}
{{- end }}
